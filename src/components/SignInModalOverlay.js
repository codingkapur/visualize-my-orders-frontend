const SignInModalOverlay = ({ handleOverlayClick }) => {
    return (
        <div className="sign-in-modal-overlay" onClick={()=> handleOverlayClick()}>

        </div>
    )
}

export default SignInModalOverlay;